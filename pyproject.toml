[project]
name = "bulk-screenshot-url"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.62.0",
    "typer>=0.27.1",
]

[project.scripts]
bulk-screenshot-url = "bulk_screenshot_url.main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
